//Space delimited
num_blocks(int) patch_size_squared(int)
//num_blocks rows that look like:
patch_size_squared doubles -> low_res and patch_size_squared doubles -> high_res
//low_res and high_res are flattened matrices in row major order
